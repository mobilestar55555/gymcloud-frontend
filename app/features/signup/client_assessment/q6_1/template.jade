div(class=s.question_number)
  | Question #{question}

div(class=s.title)=title

each option in improveOptions
  if option.text
    label(class=s.radio)=option.text
      input(type='radio', name='improve', value=option.text)
      div(class=s.check)
  else
    input(type='text', name='other_improve', placeholder='Other (Type Here)')
    input.other(type='radio', name='improve')
